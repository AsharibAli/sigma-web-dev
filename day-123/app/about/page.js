import React from "react";

const about = () => {
  return <div>About Page</div>;
};

export default about;

export const metadata = {
  title: "About Facebook - Connect with the World",
  description: "This is `About` facebook, and we can connect with world",
};
